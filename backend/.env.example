# =============================================================================
# Devin Dashboard - Backend Environment Variables
# =============================================================================

# Enterprise service user token for enterprise-scoped API endpoints.
# Provision an enterprise service user via the Devin API and paste its Bearer token here.
# See the project README.md for detailed provisioning instructions.
DEVIN_ENTERPRISE_SERVICE_TOKEN=<enterprise_service_user_token>

# Organization service user token for organization-scoped API endpoints.
# Provision an organization service user and paste its Bearer token here.
DEVIN_ORG_SERVICE_TOKEN=<org_service_user_token>

# Organization ID for organization-scoped API endpoints.
DEVIN_ORG_ID=<your_org_id>

# Legacy alias (deprecated, use DEVIN_ENTERPRISE_SERVICE_TOKEN instead)
# DEVIN_SERVICE_TOKEN=your_service_user_token_here

# Optional: FinOps configuration
# FINOPS_PRICE_PER_ACU=0.05
# FINOPS_CURRENCY=USD
