image:
  repository: devinfinops.azurecr.io/sessions-service

envFrom:
  - secretRef:
      name: devin-api-tokens

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 3

externalSecrets:
  enabled: true
  storeName: azure-key-vault
  remoteKey: devin-finops-api-tokens
