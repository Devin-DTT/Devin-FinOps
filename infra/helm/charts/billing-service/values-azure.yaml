image:
  repository: devinfinops.azurecr.io/billing-service

envFrom:
  - secretRef:
      name: devin-api-tokens

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 2

externalSecrets:
  enabled: true
  storeName: azure-key-vault
  remoteKey: devin-finops-api-tokens
